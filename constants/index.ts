/**
 * Fashionistar Design System — Barrel Export
 * theme.ts already re-exports everything from colors, typography, and spacing,
 * so we only need to export from theme here to avoid duplicate export errors.
 */
export * from './theme';
